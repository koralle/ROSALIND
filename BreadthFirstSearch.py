import sys
import InputFormatToELDigraph as iftel
import networkx as nx

def main():
    DiGraph = iftel.InputFormatToEL().input_edge_list(sys.argv[1])
    for node in range(1, len(list(DiGraph)+1):
        
        
        
if __name__ == "__main__":
    main()